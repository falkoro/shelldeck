/target
/node_modules
/uploads
/share/*.png
/.a5c
.env
*.env
.DS_Store
/links.json
/dashboard-config.json
/remote-hosts.json

# Whisper STT models (downloaded at setup, not committed)
share/models/

# runtime session state (per-instance)
dynamic-sessions.json
private-sessions.json
