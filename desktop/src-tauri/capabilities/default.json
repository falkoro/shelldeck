{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the ShellDeck thin client.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default", "window-state:default"]
}

