{
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build:frontend": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test:pins": "node scripts/test-session-pins.mjs",
    "slop:check": "bun scripts/check-ai-slop.mjs"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
