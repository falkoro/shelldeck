ShellDeck writes the sanitized share image here by default:

`shelldeck-safe-shot.png`

The image is generated by the browser's Safe shot button. It hides hostnames, paths,
commands, shell names, and captured output before saving.
